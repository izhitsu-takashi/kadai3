rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Employee collection rules
    match /employee/{employeeId} {
      allow read, write: if true; // 開発中は全許可。本番環境では適切な認証ルールを設定してください
    }
  }
}

